# Railway/Nixpacks: Azure Speech SDK needs native libs (libuuid.so.1, ALSA).
# nixPkgs alone (e.g. util-linux) does not expose libuuid.so.1 on the default loader path.
[phases.setup]
aptPkgs = ["...", "libuuid1", "libasound2", "ca-certificates"]
